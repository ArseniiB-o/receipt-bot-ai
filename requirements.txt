# Core
python-telegram-bot[job-queue]==22.6
openai==2.26.0
python-dotenv==1.0.1

# Google Sheets
gspread==6.2.1
google-auth==2.49.0

# File handling
aiofiles==25.1.0
pdfplumber==0.11.4

# Image processing (optional — for pre-resize before AI; gracefully skipped if absent)
Pillow>=10.0.0

# HTTP (already a transitive dep of openai; explicit for timeout config)
httpx>=0.27.0
certifi>=2024.2.2

# Utilities
cachetools>=5.3.0
